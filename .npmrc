# React 19: several UI deps (vaul, cmdk, react-day-picker, input-otp, ...)
# still declare React <=18 peer ranges. Use non-strict peer resolution so a
# clean install (locally and on Cloudflare Pages) doesn't abort.
legacy-peer-deps=true
